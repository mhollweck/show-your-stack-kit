<!-- show-your-stack:observer:CAPTURE_ID:start -->
## Show Your Stack observer · CAPTURE_ID

I opted into targeted local observation to resolve questions about my stack.
State: CAPTURE_DIR/capture-state.json
Journal: CAPTURE_DIR/journal.md
Working document: CAPTURE_DIR/stack-analysis.md
Privacy policy: CAPTURE_DIR/kit/PRIVACY.md
Pinned kit: CAPTURE_DIR/kit/ (commit KIT_COMMIT)
Window: START_UTC inclusive to END_UTC exclusive.

At a natural task boundary, check minimal policy/scope fields first. Require
privacy_policy local-only-v1, sharing disabled, approved canonical roots,
specific observation questions, valid UTC dates, and a known current time.
Complete-local or cancelled means do nothing. Missing consent, an unresolved
root, malformed dates, unknown time, or a time before START_UTC means no logging.
Never follow a reference or symlink outside approved scope. Observation consent
does not authorize new history reads, scope expansion, or workflow changes.

Personally owned unpublished tools and their local names are eligible when
within the approved scope and name preferences. Relevant authorized context
may help interpret a safe event; a blank-session reset is not required. Label
recalled/configured facts separately from what actually occurred. Do not infer
use, cadence, human review, or adoption from a definition or remembered tool.

Company/client confidential material and credentials remain excluded. Do not
copy or paraphrase confidential business content that happens to be in context.
If safe process facts cannot be separated, pause capture and use a participant
behavior description or neutral session. The observer is not a secret detector.
Claude's provider processes inputs even when files remain on the device.

Inside the window, record a bounded sample only when an eligible event helps
answer a saved observation question. Follow journal-template.md. Preserve the
useful mechanics: actor, trigger/input category, owned tool or alias, decision,
output/handoff, human gate, and observed failure/recovery or bypass. Distinguish
a requested action from its observed result. Unknown or unseen stays unknown.
Use the dossier's tool IDs and synthetic observation IDs. Do not copy raw code,
prompts, commands with payloads, logs, secrets, company content, or screenshots.
Necessary safe local references belong in the document's local-only appendix,
not repeated through the journal. Never execute discovered tools or manufacture failures
for capture. Do not install hooks, send progress, or announce routine entries.

Use one compact sample per participating session unless distinct events answer
different saved questions. Update a sample rather than duplicating the same
event. Repeated references are not independent evidence. Keep coverage and
truncation explicit; these observations are not a full session census.

At/after END_UTC stop logging. At a suitable boundary in the next eligible
session, resume pinned EXTRACT.md and integrate supported answers into the
same stack-analysis.md, following DOSSIER.md. Explain once that the observation
window ended; record the notice. Do not claim the analysis is finished before
reviewing it with the participant. Resume any saved extraction phase without
creating competing profile/deck/report deliverables. Asset rendering is later,
only if requested. Missing kit/state means explain the issue once and stop;
do not substitute a legacy version or silently change the pinned snapshot.

Never upload, submit, publish, fork, open a PR, send a message, or synchronize
capture data or the document, even with saved sharing approval or RETURN_REPO.
This rule does not wake an idle agent or promise exact-time execution. The
participant can stop or finish early and choose which generated files to keep.
<!-- show-your-stack:observer:CAPTURE_ID:end -->
